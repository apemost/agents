# Agents

Shared instructions and configuration for coding agents.

## Install

The project requires Python 3.14 or newer. The installer runs its Python scripts with `uv` or a prepared project `.venv`. Syncing skills also requires `npx`.

Run from the repository root:

```bash
./install.sh
```
