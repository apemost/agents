{
  "agentMode": "accept-edits",
  "allowNonWorkspaceAccess": true,
  "enableTerminalSandbox": true,
  "permissions": {
    "allow": [
      "command(cat)",
      "command(cd)",
      "command(cp)",
      "command(echo)",
      "command(find)",
      "command(gh run view)",
      "command(git diff)",
      "command(git status)",
      "command(go build)",
      "command(go vet)",
      "command(grep)",
      "command(head)",
      "command(helm dep list)",
      "command(helm dependencies list)",
      "command(helm dependency list)",
      "command(helm env)",
      "command(helm get)",
      "command(helm history)",
      "command(helm lint)",
      "command(helm list)",
      "command(helm ls)",
      "command(helm plugin list)",
      "command(helm repo list)",
      "command(helm search)",
      "command(helm show)",
      "command(helm status)",
      "command(helm template)",
      "command(helm verify)",
      "command(helm version)",
      "command(jq)",
      "command(kubectl api-resources)",
      "command(kubectl api-versions)",
      "command(kubectl cluster-info)",
      "command(kubectl describe)",
      "command(kubectl explain)",
      "command(kubectl get)",
      "command(kubectl logs)",
      "command(kubectl top)",
      "command(kubectl version)",
      "command(ls)",
      "command(make)",
      "command(mkdir)",
      "command(mv)",
      "command(npx tsc)",
      "command(pnpm build)",
      "command(pnpm exec)",
      "command(pnpm lint)",
      "command(pnpm run)",
      "command(pnpm tsc)",
      "command(pnpm vitest)",
      "command(python3 -m json.tool)",
      "command(sentry event view)",
      "command(sleep)",
      "command(sort)",
      "command(tail)",
      "command(which)",
      "command(xargs)",
      "read_url(dart.dev)",
      "read_url(developer.mozilla.org)",
      "read_url(developers.openai.com)",
      "read_url(docs.anthropic.com)",
      "read_url(docs.claude.com)",
      "read_url(docs.docker.com)",
      "read_url(docs.flutter.dev)",
      "read_url(docs.python.org)",
      "read_url(docs.sentry.io)",
      "read_url(go.dev)",
      "read_url(helm.sh)",
      "read_url(help.obsidian.md)",
      "read_url(kubernetes.io)",
      "read_url(modelcontextprotocol.io)",
      "read_url(nextjs.org)",
      "read_url(nodejs.org)",
      "read_url(pkg.go.dev)",
      "read_url(playwright.dev)",
      "read_url(pnpm.io)",
      "read_url(raw.githubusercontent.com)",
      "read_url(react.dev)",
      "read_url(readthedocs.io)",
      "read_url(tailwindcss.com)",
      "read_url(typescriptlang.org)",
      "read_url(vite.dev)",
      "read_url(wiki.archlinux.org)",
      "command(docker build)",
      "command(docker buildx)",
      "command(docker compose)",
      "command(docker exec)",
      "command(docker image inspect)",
      "command(docker image ls)",
      "command(docker info)",
      "command(docker inspect)",
      "command(docker logs)",
      "command(docker ps)",
      "command(docker pull)",
      "command(docker restart)",
      "command(docker start)",
      "command(docker stats)",
      "command(docker stop)",
      "command(docker volume inspect)",
      "command(docker volume ls)",
      "command(git add)",
      "command(git commit)",
      "command(git pull)",
      "command(go get)",
      "command(go mod)",
      "command(lsof)",
      "command(npx --no-install @playwright/cli@latest)",
      "command(npx playwright-cli)",
      "command(playwright-cli)",
      "command(pnpm add)",
      "command(pnpm dev)",
      "command(pnpm install)",
      "command(ps)",
      "command(sed)",
      "command(uv add)",
      "command(uv lock)",
      "command(uv run)",
      "command(uv sync)"
    ],
    "deny": [
      "command(docker image push)",
      "command(docker push)",
      "command(docker pussh)",
      "command(docker volume rm)",
      "command(git push)",
      "command(helm create)",
      "command(helm del)",
      "command(helm delete)",
      "command(helm dep build)",
      "command(helm dep update)",
      "command(helm dependencies build)",
      "command(helm dependencies update)",
      "command(helm dependency build)",
      "command(helm dependency update)",
      "command(helm fetch)",
      "command(helm install)",
      "command(helm package)",
      "command(helm plugin install)",
      "command(helm plugin uninstall)",
      "command(helm plugin update)",
      "command(helm pull)",
      "command(helm push)",
      "command(helm registry)",
      "command(helm repo add)",
      "command(helm repo index)",
      "command(helm repo remove)",
      "command(helm repo update)",
      "command(helm rollback)",
      "command(helm test)",
      "command(helm uninstall)",
      "command(helm upgrade)",
      "command(kubectl apply)",
      "command(kubectl attach)",
      "command(kubectl auth)",
      "command(kubectl autoscale)",
      "command(kubectl cordon)",
      "command(kubectl cp)",
      "command(kubectl create)",
      "command(kubectl delete)",
      "command(kubectl drain)",
      "command(kubectl edit)",
      "command(kubectl exec)",
      "command(kubectl expose)",
      "command(kubectl label)",
      "command(kubectl patch)",
      "command(kubectl port-forward)",
      "command(kubectl proxy)",
      "command(kubectl replace)",
      "command(kubectl rollout)",
      "command(kubectl run)",
      "command(kubectl scale)",
      "command(kubectl set)",
      "command(kubectl taint)",
      "command(kubectl uncordon)",
      "command(ssh)"
    ]
  },
  "toolPermission": "proceed-in-sandbox"
}
