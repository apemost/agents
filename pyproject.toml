[project]
name = "agents"
version = "0.1.0"
description = "Agent configuration and skill installer"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
    "tomlkit>=0.15.1",
]
